# Test Environment Configuration
NODE_ENV=test
PORT=3001

# Test Database Configuration
DATABASE_URL=postgresql://taibui@localhost:5432/quit_smoking_test

# JWT Configuration (same as dev for testing)
JWT_SECRET=test-secret-key
JWT_REFRESH_SECRET=test-refresh-secret-key
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# OpenAI Configuration (mock for testing)
OPENAI_API_KEY=test-openai-api-key

# Email Configuration (mock for testing)
EMAIL_SERVICE=resend
EMAIL_API_KEY=test-email-api-key
EMAIL_FROM=test@example.com

# Push Notifications (mock for testing)
FCM_SERVER_KEY=test-fcm-server-key
APNS_KEY_ID=test-apns-key-id
APNS_TEAM_ID=test-apns-team-id

# Security
BCRYPT_ROUNDS=12
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
